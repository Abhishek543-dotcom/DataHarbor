// src/components/Clusters.js
import React from 'react';

const Clusters = () => (
    <div>
        <h2>Cluster Management</h2>
        <p>Create and manage Spark clusters for your workloads.</p>
    </div>
);

export default Clusters;
