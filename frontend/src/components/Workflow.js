// src/components/Workflow.js
import React from 'react';

const Workflow = () => (
    <div>
        <h2>Workflow Management</h2>
        <p>Schedule jobs and link notebooks to clusters.</p>
    </div>
);

export default Workflow;
